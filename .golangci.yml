run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt

linters-settings:
  govet:
    shadow: true
  gofmt:
    simplify: true

issues:
  exclude-use-default: false
