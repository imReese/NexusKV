[package]
name = "nexus-transfer"
version = "0.1.0"
edition = "2024"
description = "Transferable KV cache memory contracts for NexusKV"
license = "Apache-2.0"

[lib]
path = "src/lib.rs"

[dependencies]
tracing = "0.1"
